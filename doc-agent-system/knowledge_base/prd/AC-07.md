# AC-07

Requirement: [[FR-04]]

Given a report has been generated, when any user with appropriate permissions views the Report History, then they can see the report metadata (generation time, parameters used, row count, file hash, delivery status) and download the artifact

Expected: Artifact is immutable — cannot be edited or deleted by any user, including admins
