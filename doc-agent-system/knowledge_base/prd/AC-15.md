# AC-15

Requirement: [[FR-09]]

Given a user is editing a report template, when they click "Preview," then the system runs the template against live data for the most recent applicable period and displays a sample of the first 100 rows plus the summary statistics

Expected: Preview clearly labeled as "Preview — not a scheduled run" and not stored as a report artifact
