# AC-11

Requirement: [[FR-06]]

Given a report template is configured for CSV output, when the report generates, then the CSV includes all matching rows (no truncation), with column headers matching the platform's export schema, UTF-8 encoding, and proper escaping of special characters

Expected: CSV opens correctly in Excel, Google Sheets, and common data tools without encoding issues
