# AC-04

Requirement: [[FR-02]]

Given a scheduled report fails during generation (e.g., timeout, data error), then the system retries twice with exponential backoff and notifies the template owner via email and in-app notification if all retries fail

Expected: Failure notification includes error type, timestamp, and a link to retry manually
