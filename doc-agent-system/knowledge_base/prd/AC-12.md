# AC-12

Requirement: [[FR-07]]

Given an IT Admin navigates to Settings > Integrations > SFTP, when they configure a new SFTP endpoint with host, port, path, and credentials, then the system validates the connection and stores credentials encrypted

Expected: Connection test result shown immediately; credentials stored using AES-256 encryption
