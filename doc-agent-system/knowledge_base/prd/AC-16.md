# AC-16

Requirement: [[FR-10]]

Given a report template uses the parameterized date range "previous calendar week" (Monday–Sunday), when the report runs on Monday June 15 at 06:00 UTC, then the data range resolves to June 8–14, inclusive, in the tenant's configured timezone

Expected: Date range shown in the report header matches the resolved range, not the parameterized expression
