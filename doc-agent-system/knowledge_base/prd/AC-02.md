# AC-02

Requirement: [[FR-01]]

Given a saved report template, when the user opens it and clicks "Load Filters," then the Search UI populates with the saved filter configuration

Expected: All filter fields match the saved configuration exactly
